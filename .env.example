# Copy to `.env` (do NOT commit `.env`).

# OpenRouter (OpenAI-compatible)
OPENROUTER_API_KEY="your_openrouter_key_here"

# Optional but recommended by OpenRouter for attribution
OPENROUTER_SITE_URL="http://localhost:3000"
OPENROUTER_APP_NAME="crime-report"

# Models (defaults are FREE variants if not set)
# OPENROUTER_CHAT_MODEL="deepseek/deepseek-r1-distill-llama-70b:free"
# OPENROUTER_IMAGE_MODEL="qwen/qwen2.5-vl-32b-instruct:free"

